# Exec Plan: Harness 自治理补强 V3

- 状态：`accepting`
- 创建：2026-08-03
- 负责人：父 Agent
- 关联方案：`方案 V3`
- 用户确认：是 — 2026-08-03，用户明确要求实施方案 V3

## 目标

在不绑定业务技术栈的前提下，为单需求、多 subagent 协作建立可机械验证的任务、确认、路径所有权、验收、安装升级和模板自检闭环。

## 实施波次

1. 配置 V2、任务治理契约与 CLI 门禁。
2. 团队配置单一真源、平台生成检查、hooks/CI 和模板发布态。
3. 安装升级、安全扫描与完整回归。

## 验收

- CLI 回归、模板/项目 doctor、fast/full/ci、任务阶段、安装升级、密钥和平台漂移用例全部通过。
- 完成后归档本目录并将 `docs/team/STATUS.md` 重置为待命。

## 决策记录

- 模板 CI 直接运行自带 Node 测试，不调用业务仓包管理器。
- 完成后的业务治理变更由同一提交中的 completed task 归档继续提供 pre-push/CI 证据，避免归档后门禁失去依据。
- 专职角色完成后，CLI 与测试的最终集成修正顺序移交父 Agent；未发生并发写入。
