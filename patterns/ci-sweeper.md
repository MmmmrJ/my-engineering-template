# CI Sweeper

An opt-in L2 assisted pattern. It requires an approved task, worktree and path lock, bounded attempt ledger, distinct maker/verifier sessions, passing project checks, and human-controlled push. Merge is always disabled in V1.
